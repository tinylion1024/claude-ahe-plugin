/**
 * Hooks entry points
 *
 * These are standalone scripts that run as hooks.
 * Import them directly for execution, not as modules.
 */
// Re-export paths for external reference
export const traceCollectorPath = __dirname + '/trace-collector.js';
export const sessionSummarizerPath = __dirname + '/session-summarizer.js';
//# sourceMappingURL=index.js.map